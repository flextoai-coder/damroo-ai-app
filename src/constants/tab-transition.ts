/**
 * Duration for AppScreen's own Reanimated slide transform (see its
 * `tabIndex` prop). The navigator itself runs no animation — see the
 * comment on `animation: 'none'` in `(tabs)/_layout.tsx`.
 */
export const TAB_SLIDE_DURATION_MS = 260;
